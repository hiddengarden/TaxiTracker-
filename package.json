{
  "name": "taxitracker-root",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\" --names \"api,app\" --prefix-colors \"yellow,cyan\"",
    "build": "npm run build --prefix backend && npm run build --prefix frontend",
    "start": "concurrently \"npm run start --prefix backend\" \"npm run preview --prefix frontend\" --names \"api,app\" --prefix-colors \"yellow,cyan\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
